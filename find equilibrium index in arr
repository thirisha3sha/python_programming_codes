# find equilibrium index in arr
''' An equilibrium index in an array is a position where
➡️ the sum of all elements on the left equals the sum of 
all elements on the right.
Input:  [1, 3, 5, 2, 2]
Output: 2
Explanation:
Index 2 → element = 5  
Left sum = 1 + 3 = 4  
Right sum = 2 + 2 = 4  
✅ Equal, so index 2 is the equilibrium index.'''  
def equilibrium_index(arr):
    right_sum=sum(arr)
    left_sum=0
    for i in range(len(arr)):
        right_sum-=arr[i]
        if left_sum==right_sum:
            return i
        left_sum+=arr[i]
    return -1
print(equilibrium_index([1,3,5,2,2]))
print(equilibrium_index([9,4,5,2,2]))